# TeslaCraftRaitingsParser

Этот скрипт автоматически собирает данные о положительных рейтингах пользователей сайта **[teslacraft.org](https://teslacraft.org)** и сохраняет их в текстовые файлы. Он использует `Selenium` и `undetected_chromedriver` для автоматизации браузера Chrome и парсинга информации с карточек пользователей.

---

## ⚙️ Возможности

- Автоматически проходит по страницам пользователей `https://teslacraft.org/members/name.{id}/card`.
- Сохраняет пользователей, у которых положительных рейтингов 100 или больше.
- Сохраняет статистику даже тех пользователей, чей профиль не доступен по обычной ссылке (профиль закрыт или пользователь был заблокирован)
- Каждые 2000 итераций перезапускает браузер, чтобы парсинг не замедлялся.
- Сохраняет текущее значение `id`, чтобы можно было продолжить с места остановки в случае, если скрипт был выключен.
- Составляет топ по рейтингам (сортирует по количеству рейтингов).

---

## 📁 Создаваемые файлы
Файлы создаются автоматически, но если необходимо начать не с 1-го айди, можно вручную создать файл first_id.txt

| Файл             | Назначение                                       |
|------------------|--------------------------------------------------|
| `first_id.txt`   | Содержит последний обработанный ID пользователя  |
| `users.txt`      | Список пользователей с рейтингом ≥ 100           |
| `users_sort.txt` | Отсортированный по рейтингу список пользователей |

---

## 🧰 Требования

Перед запускам необходимо установить зависимости из файла *requirements.txt*, используйте команду:

```bash
pip install -r requirements.txt
```

Также необходимо, чтобы на компьютере был установлен **Google Chrome**.

---

## ❗ ВАЖНО

В файле config.init надо задать путь к папке профиля хрома и максимальное значение, до которого будет производиться парсинг.

Ниже пример МОЕГО пути к папке Chrome. Скорее всего, у вас отличается только имя пользователя после *Users*. *SeleniumProfile* — название папки, которая будет создана автоматически.
```ini
UserDataDir = C:\Users\Misha20062006\AppData\Local\Google\Chrome\SeleniumProfile
MaxID = 132792
```

- **UserDataDir** — путь к профилю Chrome, который будет использоваться Selenium  
  (он позволяет сохранить сессию и обойти капчу, если она была введена ранее).
- **MaxID** — айди последнего пользователя на форуме. Это значение не равно количеству пользователей на всём форуме. Его необходимо указать, ориентируясь на какого-нибудь последнего зарегистрированного пользователя форума.
---

---

## ⚠️ Возможные ошибки
При первом запуске необходимо вручную пройти капчу CloudFlare в окне браузера. Наиболее вероятно, что снова капча не появится, если не разрывать соединение (не включать VPN, переключаться на другой Wi-Fi и т. д.).

Иногда Chrome ведёт себя странно. Он может не открыться. Потребуется удалить папку *SeleniumProfile* из пункта "ВАЖНО".\
Следовательно, лучше не оставлять процесс сбора статистики полностью без присмотра.

